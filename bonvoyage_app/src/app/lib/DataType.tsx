import { IconType } from "react-icons";
export interface history_record {
    date: string;
    destination: string;
    city: string;
    type: string;
}
export interface place_types{
    name:string,
    selected:boolean
}